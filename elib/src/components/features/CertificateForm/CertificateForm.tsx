import React from 'react';
import { useForm } from 'react-hook-form';

type CertificateForm = {};

export const CertificateForm = (props: Props): React.JSX.Element => {
  const {} = useForm();

  return <form></form>;
};
